# Supabase — base secrets and local defaults.
# On first setup, apply-env copies from server/supabase/.env.example via scripts/ensure-env.sh.
# Then merge env.dev.example or env.prod.example from this folder.
