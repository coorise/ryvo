# Supabase — VPS (merged into server/supabase/.env via apply-env.sh)
# Base secrets: server/supabase/.env.example (ensure-env.sh)
# Do NOT set KONG_HTTP_PORT — API is exposed via Caddy only.

SUPABASE_PUBLIC_URL=https://ryvo-line-server.dev.agglomy.com
API_EXTERNAL_URL=https://ryvo-line-server.dev.agglomy.com
SITE_URL=https://ryvo-line.dev.agglomy.com
ADDITIONAL_REDIRECT_URLS=https://ryvo-line.dev.agglomy.com/*,https://ryvo-line-admin.dev.agglomy.com/*

# External providers — set once in server/supabase/.env (not overwritten by empty templates)
# GOOGLE_MAPS_API_KEY=
